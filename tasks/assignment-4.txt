1. create a class to represent an employee
data: name, id, basicPayment, daPayment, hraPayment
constructors: default and parameterized (for all the data members)
methods: to calculate total salary
	arguments: None
	return type: float

2. from main method, use this class to create few instances
	a. ask user how many instances to create
	b. create an array to store references of the instances of the employees
   c. create instances and store each of them in the array
   d. iterate through the array and pick up one employee reference at a time. call method to calculate salary and print the salary along with the name of the employee
   format: <emp-name> has salary of <salary-value>